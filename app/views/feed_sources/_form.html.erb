<%= form_for(@feed_source) do |f| %>
  <% if @feed_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feed_source.errors.count, "error") %> prohibited this feed_source from being saved:</h2>

      <ul>
      <% @feed_source.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url, 'Feed Url' %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
