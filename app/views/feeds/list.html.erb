<h2>Feeds</h2>
<%= link_to 'Edit Sources', feed_sources_path%>
<ul>
  <%@entry.each do |feed|%>
    <li>
     <%= link_to "#{feed.title.sanitize}", "#{feed.url}",:target => "_blank" %>
     <%#=feed.author%>   
     <%#=feed.published%> 
    </li>
 <% end %>
</ul>